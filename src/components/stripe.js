import React from "react";
import "@stripe/stripe-js";
import StripeCheckout from "react-stripe-checkout";
class StripeButton extends React.Component {

  //booking id as prop so we can this.props.booking
  // post bookingservices data. set pricinv variable. 


 

  render() {
  return null
    // return (
    //   <div>
    //     <button onClick={this.getStripeKey} id="stripe">
    //       pay
    //     </button>
    //   </div>
    // );
  }
}

export default StripeButton;
